#version 330 core

// Interpolated values from the vertex shaders
// in vec3 fragmentColor;

// Ouput data
out vec3 color;

void main()
{
    // Custom color
    // color = fragmentColor;
    color = vec3(0.2,0.5,0.2);
}
